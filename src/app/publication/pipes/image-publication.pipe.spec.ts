import { ImagePublicationPipe } from './image-publication.pipe';

describe('ImagePublicationPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePublicationPipe();
    expect(pipe).toBeTruthy();
  });
});
